// import { playerConstants } from '../actions/player_constants';

const initialState = {

};

const PlayerReducer = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export default PlayerReducer;
